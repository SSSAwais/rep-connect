import React from "react";
import "./loading.css";
const Loading = () => {
  return (
    <div className="col">
      <span className="cart-loading">Loading</span>
    </div>
  );
};

export default Loading;
